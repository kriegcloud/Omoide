import { styled } from "@mui/material/styles";

// Unlike Box, a styled img forwards width/height as intrinsic HTML attributes
// while sx controls the responsive display size.
export const MediaImage = styled("img")({});
